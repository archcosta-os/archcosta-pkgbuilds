pkgname=archcosta-skeleton
pkgver=1.0
pkgrel=8
pkgdesc="Default skeleton files for ArchCosta"
arch=('any')
url="https://github.com/archcosta-os/archcosta-pkgbuilds"
license=('GPL3')
source=()
sha256sums=()

package() {
    mkdir -p "$pkgdir/etc/skel"
    cp -r "$startdir/skel/." "$pkgdir/etc/skel/"
}
