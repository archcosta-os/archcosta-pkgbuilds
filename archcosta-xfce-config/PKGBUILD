pkgname=archcosta-xfce-config
pkgver=1.0
pkgrel=8
pkgdesc="XFCE4 configuration files for ArchCosta"
arch=('any')
url="https://github.com/archcosta-os/archcosta-pkgbuilds"
license=('GPL3')
source=()
sha256sums=()

package() {
    mkdir -p "$pkgdir/etc"
    cp -r "$srcdir/../etc/." "$pkgdir/etc/"
}
