pkgname=archcosta-cinnamon-settings
pkgver=1.0
pkgrel=1
pkgdesc="ArchCosta Cinnamon configuration files"
arch=('any')
url="https://github.com/archcosta-os/archcosta-pkgbuilds"
license=('GPL3')
source=()
sha256sums=()
package() {
    [ -d "$startdir/archcosta-cinnamon-settings/etc" ] && cp -r "$startdir/archcosta-cinnamon-settings/etc" "$pkgdir/"
    [ -d "$startdir/archcosta-cinnamon-settings/usr" ] && cp -r "$startdir/archcosta-cinnamon-settings/usr" "$pkgdir/"
}
